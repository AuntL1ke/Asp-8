@{
    ViewData["Title"] = "Home Page";
}
@model List<Car>
<div class="d-flex flex-wrap">
    @foreach (Car car in Model)
    {
        <div class="card m-1" style="width: 17rem">
            <img />

            <div class="card-body">
               @*   <div class="container" style="width: 100%; height: 120px;">
                     <img class="card-img-top" style="width: 100%; height: 100%; object-fit: cover" src="@car.ImagePath" alt="@car.Model image" />
                 </div> *@

                <div class="container m-1">
                    <h5 class="card-title">@car.Model</h5>
                    <p class="card-text">@car.Color</p>
                    <p class="card-text">@car.Year</p>
                    <p class="card-text text-primary">@car.Category.Name</p>

                </div>

                <div class="btn-group">
                   @*  @if (car.IsInCart)
                    {
                        <a class="btn btn-primary disabled" asp-controller="Cart" asp-action="Add" asp-route-id="@car.Id">Added in Cart</a>
                    }
                    else
                    { *@
                    <a class="btn btn-primary" asp-controller="Cart" asp-action="Add" asp-route-id="@car.Id">Add to Cart</a>
                    

                   @*  @if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
                    {
                        <a class="btn btn-primary" asp-controller="Cars" asp-action="Edit" asp-route-id="@car.Id">Edit</a>
                        <a class="btn btn-danger" asp-controller="Cars" asp-action="Delete" asp-route-id="@car.Id">Delete</a>
                    } *@
                </div>
            </div>
        </div>
    }
</div>

